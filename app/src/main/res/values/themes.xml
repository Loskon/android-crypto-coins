<resources>
    <!-- Base application theme. -->
    <style name="Theme.CryptoCoins" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/orange</item>
        <item name="colorPrimaryVariant">@color/orange</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/orange</item>
        <item name="colorSecondaryVariant">@color/orange</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:statusBarColor">@color/smoke</item>
        <!-- Disable overScrollMode.  -->
        <item name="android:overScrollMode">never</item>
        <!-- Change snackbar margins.  -->
        <item name="snackbarStyle">@style/MarginSnackbarStyle</item>
    </style>
</resources>